alembic
asyncpg
# Core Web Framework
fastapi

# HTTP Client
httpx

# AI Stack (Latest Compatible Versions)
# NOTE: LangChain/LangGraph dependencies are ONLY used for LangSmith tracing (optional observability)
# Actual AI implementation uses OpenAI SDK directly (AsyncOpenAI) with custom multi-agent system
langchain  # Required only for LangSmith integration (src/core/tracing.py)
langchain-openai  # Minimal usage - only in optional PII detector fallback
langgraph  # Listed for compatibility but NOT actively used
langsmith  # Optional: AI observability and tracing (gracefully degrades if unavailable)
openai  # PRIMARY AI DEPENDENCY - All LLM calls use AsyncOpenAI client directly

# Monitoring
prometheus-client
pydantic
pydantic-settings

# Document Processing
pypdf

# Image Processing
Pillow

# Testing
pytest
pytest-asyncio
pytest-cov

# Environment & Config
python-dotenv

# Utils
pyyaml

# Vector Store
qdrant-client

# Cache
redis[asyncio]>=5.0.0

# Text Processing
sentence-transformers

# Epic 3 - Pattern Retrieval
rank-bm25>=0.2.2
tenacity>=8.2.0

# Database
sqlalchemy
psycopg2-binary
greenlet

# Server-Sent Events
sse-starlette

# Epic 6 - Production Infrastructure
boto3>=1.28.0

# Epic 8 - Regeneration
celery[redis]>=5.3.0

# Epic 3 - Safety & Guardrails
nh3>=0.2.0
python-magic>=0.4.27
slowapi>=0.1.9

# Document Processing
unstructured[pdf]
uvicorn[standard]

# Jupyter and Data Science
jupyter
ipykernel
matplotlib
pandas
scipy
seaborn
plotly
notebook
nest-asyncio
